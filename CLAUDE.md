@AGENTS.md

# Start here

Before doing anything, read **@PROJECT_LOG.md** — the living status of this build (what
exists, what was decided, what's still open). Keep it updated as work progresses.

If **SESSION_HANDOFF.md** exists in the repo root (local, gitignored), read it too — it holds
the operational continuity notes (exact build/commit/deploy commands, auth state, the
reference-doc workflow, known TODOs) needed to resume mid-stream. Keep it updated as you work.
