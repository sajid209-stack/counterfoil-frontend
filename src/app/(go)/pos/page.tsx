/** The till itself is rendered by `pos/layout.tsx`, which stays mounted across
 *  `/pos` and `/pos/cart` so one sale survives the move between them. This
 *  file exists to declare the route. */
export default function PosPage() {
  return null;
}
