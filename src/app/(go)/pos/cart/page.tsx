/** The cart page. Rendered by `pos/layout.tsx` — see the note there: the sale
 *  lives in one component mounted at the layout, so moving between the grid
 *  and the cart is a real navigation that does not rebuild it. */
export default function CartPage() {
  return null;
}
